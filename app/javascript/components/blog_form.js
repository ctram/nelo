import React from 'react';
import constants from '../constants';

export default class Navbar extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    const { blogContent, blogTitle } = this.props
    
    return (
      <div>

      </div>
    );
  }
}
